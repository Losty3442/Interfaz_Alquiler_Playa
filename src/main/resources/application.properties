# Server Configuration
server.port=${PORT:8080}

# Spring Application Name
spring.application.name=AlquilerPlayaWeb

# Thymeleaf Configuration
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Database Configuration (PostgreSQL - Supabase Session Pooler)
# Uses Session Pooler for IPv4 compatibility (works with Railway)
# Note: ConexionDB reads this directly, so we use plain values here
# Environment variables are checked first in ConexionDB.java
spring.datasource.url=jdbc:postgresql://aws-0-us-west-2.pooler.supabase.com:6543/postgres?sslmode=require
spring.datasource.username=postgres.goehhhwusdoduduxntgq
spring.datasource.password=G+M.*2caUwa?SiS
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP Configuration optimizada para velocidad
spring.datasource.hikari.connection-timeout=10000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=3
spring.datasource.hikari.idle-timeout=120000
spring.datasource.hikari.max-lifetime=300000
spring.datasource.hikari.pool-name=AlquilerPlayaPool

# Logging
logging.level.org.springframework.web=INFO
logging.level.com.playa.alquiler=DEBUG
